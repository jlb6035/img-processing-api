import images from "../../../routes/api/images";

describe('Test Image Services', () => {
    it('test convert file name',  () => {
        expect(images.convertFileName());         //Not sure why this isn't working...
    }
)});